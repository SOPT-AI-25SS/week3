// Minimal stub to satisfy `/// <reference types="long" />` directives.
// The actual Long class lives in the `long` package and is imported where
// required. This file merely prevents TypeScript compiler errors during the
// global type resolution phase.

export {}; // No global declarations.
